- file:
    path: /leihs/config
    state: directory
    recurse: true
    owner: root

- include: master_secret.yml

- include: deploy_info.yml
  tags: [deploy_info]

- include: database.yml

- command: systemctl daemon-reload
  changed_when: false
  name: reload systemctl
