#!/bin/bash -e

DEPLOY_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"

python3 -m venv ${DEPLOY_DIR}/tmp/py-venv
source ${DEPLOY_DIR}/tmp/py-venv/bin/activate
pip install -r ${DEPLOY_DIR}/ansible-requirements.txt
