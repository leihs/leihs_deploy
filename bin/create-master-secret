#!/usr/bin/ruby
require 'securerandom'

if File::exist? "/leihs/config/master-secret.txt"
  exit 0
else
  IO::write "/leihs/config/master-secret.txt", SecureRandom.base64(32)
  File.chmod(0644,"/leihs/config/master-secret.txt")
  exit 0
end
