#!/usr/bin/env bash

echo '✪ virtualenv: preparing…   [if this fails, run `./bin/virtualenv-install` to debug]'

source bin/virtualenv-install -qqq &>/dev/null \
  || source bin/virtualenv-install \
  || exit 1

echo '✪ virtualenv: OK'
